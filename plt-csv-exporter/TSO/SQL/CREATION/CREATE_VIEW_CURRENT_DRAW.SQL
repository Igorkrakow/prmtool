-- to be created in TXSTORE schema

CREATE VIEW TXSTORE.LAST_CLOSED AS
SELECT IDDGGAME, MAX(DRAWNUMBER) DRAWNUMBER, MAX(DRAWCLOSETIME) DRAWCLOSETIME FROM
    GIS.DGGAMEEVENT WHERE STATE = 'CLOSED'
GROUP BY IDDGGAME;

